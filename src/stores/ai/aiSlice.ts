// AI Slice - placeholder
import { createSlice } from '@reduxjs/toolkit'

const aiSlice = createSlice({
  name: 'ai',
  initialState: { enabled: true },
  reducers: {}
})

export default aiSlice.reducer
